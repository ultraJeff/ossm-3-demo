apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Integrated deployment: uses shared operators and MinIO
# from cluster-o11y-operator-demo (deployed via observability ArgoCD app).
# UIPlugins are also provided by cluster-o11y.
# Only SM3-specific operators (ServiceMesh3, Kiali) are included here.
resources:
- ../../base
- operators
- s3-secret.yaml
