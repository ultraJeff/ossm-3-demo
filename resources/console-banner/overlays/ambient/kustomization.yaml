apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base

patches:
- target:
    kind: ConsoleNotification
    name: service-mesh-banner
  patch: |-
    - op: replace
      path: /spec/text
      value: "🌐 Service Mesh 3 — Ambient Mode"
    - op: replace
      path: /spec/backgroundColor
      value: "#FFD700"
    - op: replace
      path: /spec/color
      value: "#000000"

