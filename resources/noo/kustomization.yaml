apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Network Observability infrastructure
# Note: Operators must be installed separately first

resources:
- namespace.yaml
- minio.yaml
- lokistack.yaml
- flowcollector.yaml

