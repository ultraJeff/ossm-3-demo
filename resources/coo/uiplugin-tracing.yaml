# Distributed Tracing UI Plugin
# Adds Observe → Traces page to the OpenShift Console
# Connects to TempoStack instances for trace visualization
apiVersion: observability.openshift.io/v1alpha1
kind: UIPlugin
metadata:
  name: distributed-tracing
spec:
  type: DistributedTracing
  distributedTracing:
    timeout: 30s

