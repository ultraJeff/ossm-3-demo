apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# RHOAI AI model serving — ServingRuntimes + InferenceServices
# Requires: RHOAI base applied, model images pushed to internal registry

resources:
  - ../../base
  - mlserver-serving-runtime.yaml
  - vllm-serving-runtime.yaml
  - iris-inference-service.yaml
  - phi4-inference-service.yaml
  - httproutes.yaml
  - ocp-routes.yaml
