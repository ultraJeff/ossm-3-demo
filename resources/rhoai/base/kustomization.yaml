apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# RHOAI base — namespace, DSCInitialization, DataScienceCluster, waypoint proxy
# Requires: RHOAI operator installed, OSSM3 ambient mode active

resources:
  - namespace.yaml
  - dsci.yaml
  - dsc.yaml
  - waypoint.yaml
  - peer-authentication.yaml
  - test-client.yaml
