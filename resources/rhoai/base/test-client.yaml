apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-client
  namespace: ossm-ai-models
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-client
  template:
    metadata:
      labels:
        app: test-client
    spec:
      containers:
        - name: curl
          image: curlimages/curl
          command: ["sleep", "infinity"]
