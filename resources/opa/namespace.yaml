# Namespace for BookInfo with OPA authorization
apiVersion: v1
kind: Namespace
metadata:
  name: bookinfo-opa
  labels:
    istio.io/dataplane-mode: ambient
    # OPA external authorization still uses sidecar injection for the OPA filter
    opa-istio-injection: enabled
