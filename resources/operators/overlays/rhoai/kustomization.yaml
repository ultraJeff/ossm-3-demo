apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Red Hat OpenShift AI (RHOAI) operator
# Optional — only needed for AI model serving demos

resources:
  - namespace.yaml
  - subscription.yaml
