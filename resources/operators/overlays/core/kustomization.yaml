apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Core operators required for OSSM3 demo
# Includes: Service Mesh 3, Kiali, Tempo, OpenTelemetry

resources:
  - namespaces.yaml
  - subscriptions.yaml
