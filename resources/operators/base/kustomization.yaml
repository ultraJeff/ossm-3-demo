apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base resources shared by multiple operator overlays
# Currently empty - overlays define their own operator-specific resources
resources: []
