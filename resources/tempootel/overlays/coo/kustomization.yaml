apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# COO-optimized Tempo/OTEL configuration
# - Tempo with gateway enabled for OpenShift Console "Observe → Traces" UI
# - Multi-tenant with RBAC disabled for simplified access
# - OTEL collector with bearer token authentication to gateway

resources:
- ../../base
- tempo.yaml
- opentelemetrycollector.yaml
- otel-collector-rbac.yaml
- namespace-opentelemetrycollector.yaml

